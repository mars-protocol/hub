run:
  tests: false
  timeout: 5m # timeout for analysis, e.g. 30s, 5m, default is 1m

linters:
  disable-all: true
  enable:
    - depguard
    - dogsled
    - exportloopref
    - errcheck
    - goconst
    - gocritic
    - gofumpt
    - gosec
    - gosimple
    - govet
    - ineffassign
    - misspell
    - nakedret
    - prealloc
    - staticcheck
    - stylecheck
    - revive
    - typecheck
    - unconvert
    - unused
    - misspell
    - nolintlint
